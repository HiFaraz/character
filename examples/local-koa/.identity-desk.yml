database:
  dialect: sqlite
  logging: false
  url: $DATABASE_URL
authenticators:
  local:
    module: identity-desk-local
    source: npm
    version: ^0.1.0
    successRedirect: '/restricted'
session:
  keys: $SESSION_KEYS